const { Clutter, St } = imports.gi;
const Main = imports.ui.main;

var Photo = class Page extends St.Bin {
    constructor(props) {
        super(props);
        // let bin1 = new St.Bin({height:200, width:200, style_class:'test', y_fill:false})
        // bin1.set_child(window);
        // bin1.set_child(new St.Bin({height:20, width:20, style_class:'test2'}))
        // bin1.set_child(clone);
        // bin1.set_size(200,200);

    }
}